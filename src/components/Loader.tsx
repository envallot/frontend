import React from 'react';
import { Container, Typography } from '@material-ui/core';

export default function Loader() {
  // Loader is removed for now
  return (
    <Container>
      <Typography variant="h3" component="h1">
        
      </Typography>
    </Container>
  )

}